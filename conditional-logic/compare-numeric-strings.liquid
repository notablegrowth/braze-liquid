{% comment %}
Casting a string as a number and then doing a comparison against another value.
{% endcomment %}

{% if {{yoy_change}} contains '-' %}
    {% assign is_negative = true %}
{% else %}
    {% assign is_negative = false %}
{% endif %}

{% if {{ yoy_change | times: 1 }} >= 200 %}
    {% abort_message('high_low_values') %}
{% elsif is_negative == true && {{ yoy_change | replace: '-', '' | times: 1 }} > 97 %}
    {% abort_message('low_value') %}
{% endif %}
